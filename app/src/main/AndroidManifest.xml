<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.cryptoadvisor3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cryptoadvisor3"
        tools:targetApi="31">

        <!-- 🔹 Ekran dodawania kryptowaluty ze strzałką powrotu -->
        <activity
            android:name=".AddCryptoActivity"
            android:exported="false"
            android:parentActivityName=".WalletActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".WalletActivity" />
        </activity>

        <!-- 🔹 Ekran wykresu -->
        <activity android:name=".ChartActivity" android:exported="false" />

        <!-- 🔹 Ekran portfela -->
        <activity android:name=".WalletActivity" android:exported="false" />

        <!-- 🔹 Edukacja -->
        <activity android:name=".EducationActivity" android:exported="false" />

        <!-- 🔹 Lista kryptowalut -->
        <activity android:name=".CryptoListActivity" android:exported="false" />

        <!-- 🔹 Rejestracja -->
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- 🔹 Ekran główny -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- 🔹 Logowanie (startowa aktywność aplikacji) -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
